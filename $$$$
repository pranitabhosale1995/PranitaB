useEffect(() => {
    // Range tab madhe GLCC compulsory aslyamule check kara
    if (activeTab === 'range' && rangeGlcc) {
        fetchDifferences(); 
    }
}, [rangeGlcc, startDate, endDate, activeTab, fetchDifferences]); 
